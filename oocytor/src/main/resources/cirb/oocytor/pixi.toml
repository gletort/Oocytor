
[workspace]
authors = ["Gaelle Letort"]
channels = ["pytorch", "nvidia", "conda-forge"]
name = "oocytor-env"
platforms = ["linux-64"]
version = "0.0.1"

[tasks]

[dependencies]
python = "3.10.*"
appose = ">=0.10.1,<0.12"

[pypi-dependencies]
sahi = "*"
ultralytics = "*"
scikit-image = "*"
